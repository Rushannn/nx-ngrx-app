export interface Profile {
  id: number;
  username: string;
  name: string;
  surname: string;
  birthday: string;
  city: string;
}

export const CITIES = ['Kazan', 'Los Angeles', 'Chicago', 'Houston', 'Phoenix', 'Philadelphia'];
